#include <stdlib.h>
#include <stdio.h>
#include <string.h>

/**
 *_strcpy - copies string
 *
 *@a : string
 *@n : string
 *
 *Return: string
*/
char *_strcpy(char *a, char *n)
{
	i = 0;
	while (src[i] != '\0')
		i++;
	sz = i;
	for (i = 0; i <= sz; i++)
	{
		des[i] = src[i];
	}
	return (des);
}
